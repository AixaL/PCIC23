a_tabs.forEach((aTag) => {
  // Get the href and innerText
  const href = aTag.href;
  const innerText = aTag.innerText;
  
  // Do something with the href and innerText
  console.log(innerText , href);
});

let a_tabs = document.querySelectorAll('.x1sfzahb.x1odaek6')